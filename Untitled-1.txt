todos os puzzles são quadrados?
built in functions:
    include/3 (condição, Lista, lista resultado); inclui na lista 
resultado os elementos da lista que satisfazem a condição;
    exclude
    append/3 append LST1 in LST2 ; results in LST3;
    append/2 (list of lists, list complete); transforma uma lista
de listas numa só lista;
    findall/3 findall(N,condição,Lista de nrs); encontra todos os que 
satisfazem a condição posta;
    subtract/3 subtrai de L1 L2 e dá L3; subtract(L1,L2,L3);
    between/3 (limite inferior,limite superior, N);
    lenght/2 (Lista ,Comp);
    nth1 (Pos,Lista,Elemento); confirma que o elemento está na 
posição da Lista
    maplist/3 (transformação, lista original ,lista resultado)  ; 
transforma os elementos da lista original;
    

